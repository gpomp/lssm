<div class="themes formule-ctn">
   <h2>{{ post.titre_themes }}</h2>
   <div class="cat-descr">{{ post.description_themes }}</div>
   <div class="list">
       <ul>
            {% for item in post.get_field('themes') %}
                <li>
                    <div class="img-holder">
                      <div class="image"><img src="{{ TimberImage(item.image).src }}" alt="{{ TimberImage(item.image).alt }}"/></div>
                    </div>
                    <div class="descr">
                        <h3>{{item.titre}}</h3>
                        <p>{{item.description}}</p>
                    </div>
                </li>
            {% endfor %}
       </ul>
   </div>

</div>
