<header class="banner navbar navbar-static-top" role="banner">
  <div class="container">
    <div class="head-contacts">
        <div class="contacts">
            <a href="mailto:{{default.email}}">{{default.email}}</a>
        </div>

        <div class="social"></div>
    </div>
    <div class="logo"><a href="{{TimberPost(4).link}}"><img src="{{theme.link}}/img/logo.jpg" alt="logo" /></a></div>
    <nav class="main-nav content" role="navigation">
            <div>

                <ul>
                {% for item in menu.items %}
                    <li id="menu-item-{{item.ID}}" class="menu-item menu-item-type-{{item.type}} menu-item-object-{{item.object}} menu-item-{{item.ID}} {{item.slug}} {% if post.slug == item.slug %} current {% endif %}"><a href="{{item.link}}">{{item.title}}</a></li>
                {% endfor %}
                    {% if function('getObjectListExist') %}
                    <li id="menu-item-{{menu.items|length}}" class="menu-item menu-item-{{menu.items|length}} {% if post.slug == 'wishlist' %} current {% endif %}"><a href="{{TimberPost(270).link}}"><span class="icon-basket"></span> Ma Liste</a></li>
                    {% endif %}
                </ul>
                
            </div>
    </nav>
  </div>
</header>
