{% extends 'layouts/base.twig' %}

{% block content %}
    <div id="primary" class="content-area">
        <main id="main" class="site-main" role="main">
            <div class="loc-wrapper">
              <h1>{{ post.title }}</h1>
              <div>{{ post.content }}</div>
              <div class="grid-ctn">
                {% for cat in categories %}
                  <div class="gr-4">
                    <a href="{{function('get_permalink', cat.id)}}/page/1" style="background:url({{ TimberImage(cat.image).src }}) no-repeat center; background-size:cover;">
                      <div class="inner">
                        {{ cat.title }}
                      </div>
                    </a>
                  </div>
                  
                {% endfor %}
              </div>
              <div class="grid-ctn">
                <div class="gr-12"><a class="round-btn" href="{{function('get_permalink', function('get_page_by_path', 'prestations'))}}">Revenir aux préstations</a></div>
              </div>
            </div>
        </main><!-- #main -->
    </div><!-- #primary -->
{% endblock %}
